MRF Pool Exhaustion & Residue Framework (PERF)
池耗尽与池残留框架设计规范 v1.0

一、框架定位
1.1 核心定义
池耗尽框架：管理可恢复资源池的消耗顺序与故障转移策略。
池残留框架：提供资源耗尽后的可观测性，不强制记录，但保证可查询。

1.2 设计哲学
原则	说明
被动可观测	不主动记录，需要时能查到
策略可插拔	耗尽策略可配置（负载均衡/优先耗尽/加权）
向上收敛	池空时自动向上层降级
零依赖	核心实现无外部依赖
与 MRF 公理一致	失败即不存在、失败即重试、写成功即主
二、核心架构
2.1 组件模型
text
┌─────────────────────────────────────────────────────────┐
│                    Pool Manager                          │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   Strategy  │  │   Residue   │  │  Observer   │     │
│  │   Selector  │  │   Tracker   │  │   Query     │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │              Resource Pool (Active)              │   │
│  │  [R1] [R2] [R3] [R4] [R5]                       │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Residue Pool (Inactive)             │   │
│  │  {R1: {reason, time, expectRecover}}            │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
2.2 核心接口
javascript
interface PoolManager {
  // 核心方法
  acquire(): Resource                    // 获取下一个资源
  release(resourceId, success): void     // 释放并报告结果
  
  // 策略配置
  setStrategy(strategy: Strategy): void  // 负载均衡/优先耗尽/加权
  
  // 可观测性
  queryResidue(resourceId): Residue|null // 查询残留
  getPoolStatus(): PoolStatus            // 获取池状态
  
  // 恢复管理
  recover(resourceId): void              // 手动恢复资源
  autoRecovery(strategy: RecoveryStrategy): void  // 自动恢复策略
}
三、耗尽策略
3.1 策略类型
策略	行为	适用场景
RoundRobin	轮流使用，均匀分配	资源对等，希望延长整体寿命
PriorityExhaustion	优先耗尽当前资源	账号配额管理，保护其他资源
Weighted	按权重比例分配	资源能力不同
LeastUsed	优先使用最空闲的	追求最低延迟
3.2 策略配置示例
javascript
// 负载均衡
pool.setStrategy('round_robin');

// 优先耗尽（你的代码使用的模式）
pool.setStrategy('priority_exhaustion', {
  order: 'sequential',  // 按顺序耗尽
  resetOnRecovery: true // 恢复后重置顺序
});

// 加权分配
pool.setStrategy('weighted', {
  weights: { 'acc1': 10, 'acc2': 1, 'acc3': 1 }
});
四、残留管理
4.1 残留数据结构
javascript
interface Residue {
  resourceId: string | number;     // 资源唯一标识
  exhaustedAt: number;             // 耗尽时间戳
  reason: string;                  // 耗尽原因（QUOTA/NETWORK/ERROR）
  expectedRecovery?: number;       // 预期恢复时间（可选）
  convergenceTo?: string;          // 收敛到的上层资源
  metadata?: Record<string, any>;  // 扩展元数据
}
4.2 残留查询
javascript
// 按需查询，不强制记录
const residue = await pool.queryResidue('account_1');
if (residue) {
  console.log(`账号耗尽了：${residue.reason}`);
}

// 批量查询
const residues = await pool.queryResidues({
  startTime: Date.now() - 24 * 3600 * 1000,
  reasons: ['QUOTA_EXHAUSTED']
});
4.3 残留存储后端（可插拔）
javascript
// 内存存储（默认）
const pool = new PoolManager(resources, {
  residueStorage: 'memory'
});

// KV 存储（生产环境）
const pool = new PoolManager(resources, {
  residueStorage: new KVStorage(env.KV)
});

// 不存储（只暴露接口，用户自己决定）
const pool = new PoolManager(resources, {
  residueStorage: null  // 残留可查询但不存储
});
五、恢复机制
5.1 恢复策略
策略	行为	适用场景
DailyReset	每日全量恢复	账号配额（你的代码）
Cooldown	冷却后单个恢复	限流令牌
OnSuccess	成功时即时恢复	网络闪断
Progressive	渐进恢复，避免冲击	大规模资源池
Manual	手动恢复	需要人工介入
5.2 恢复配置示例
javascript
// 每日重置（你的代码）
pool.setRecoveryStrategy('daily', {
  resetHour: 0,  // 每天0点
  timezone: 'Asia/Shanghai'
});

// 冷却恢复
pool.setRecoveryStrategy('cooldown', {
  duration: 30 * 60 * 1000,  // 30分钟
  maxRetries: 3
});

// 渐进恢复
pool.setRecoveryStrategy('progressive', {
  interval: 60 * 1000,  // 每分钟恢复一个
  batchSize: 1
});
六、收敛规则
6.1 向上收敛
javascript
// 池空时自动向上收敛
async function acquire() {
  const resource = await pool.acquire();
  if (!resource) {
    // 池空，向上收敛到上层
    return await upperLayer.acquire();
  }
  return resource;
}
6.2 收敛路径可观测
javascript
// 记录收敛路径
const residue = {
  resourceId: 'account_1',
  exhaustedAt: Date.now(),
  convergenceTo: 'L3_cache_pool',  // 收敛到哪一层
  convergencePath: 'L4_accounts → L3_cache → L2_default'
};
七、与 MRF 公理的对齐
公理	池耗尽框架的体现
写入成功 = 成为主	资源获取成功 = 成为当前使用资源
失败 = 主不存在	资源失败 = 从活跃池移除
失败 = 重试	资源失败 = 切换到下一个
被动响应	不主动探测健康，只在失败时反应
可观测 > 永生	残留可查询，不强制全局记录
八、完整使用示例
javascript
// 初始化池
const pool = new PoolManager(ACCOUNTS, {
  strategy: 'priority_exhaustion',  // 优先耗尽
  recovery: 'daily',                 // 每日恢复
  residueStorage: new KVStorage(env.KV)  // 可选存储
});

// 使用池
async function handleRequest() {
  const account = await pool.acquire();
  if (!account) {
    // 池空，向上收敛
    return await fallbackHandler();
  }
  
  try {
    const result = await callAPI(account);
    await pool.release(account.id, true);  // 成功
    return result;
  } catch (error) {
    await pool.release(account.id, false); // 失败
    
    // 可选：查询残留
    const residue = await pool.queryResidue(account.id);
    if (residue) {
      logger.warn(`账号 ${account.id} 已耗尽`, residue);
    }
    
    // 重试
    return handleRequest();
  }
}
九、规范总结
特性	规范要求
策略	可插拔，默认支持 4 种
残留	可查询，不强制记录
恢复	可配置，支持 5 种策略
收敛	向上收敛，路径可观测
依赖	零依赖，存储可插拔
哲学	被动可观测 > 全局记录
十、版本与演进
v1.0 (当前)：核心池耗尽 + 池残留框架

v1.1 (规划)：支持混合策略（层内池 + 层间肢端）

v1.2 (规划)：可观测性增强（残留导出、指标聚合）



对于高可恢复资源 按固定频率恢复的资源 建议使用池耗尽 作为肢端耗尽的抽象独立和有益补充 可并行 可混合

适用池耗尽的池的边界定义

 池耗尽的三个必要条件
条件	说明	反例
1. 存在调度者	有人/系统在管理这个池	裸机单进程，自己调度自己
2. 我们能识别调度者	我们知道谁在管，能与之交互	黑盒硬件调度器，不暴露接口
3. 我们能控制调度者	我们能影响调度决策	操作系统内核，你改不了调度算法
三个条件必须同时满足，才属于“池耗尽”的管理范围。

🔍 场景分析
资源	有调度者	能识别调度者	能控制调度者	属于池耗尽？
CPU（用户进程视角）	✅ OS内核	✅ 知道	❌ 不能（改不了内核调度）	❌ 不属于
CPU（K8s Pod视角）	✅ K8s调度器	✅ 知道	✅ 能（通过requests/limits）	✅ 属于
Cloudflare Worker	✅ CF平台	⚠️ 部分知道	❌ 不能（平台黑盒）	⚠️ 不属于（你只能接受）
你的5个账号Worker	✅ MRF调度器	✅ 知道	✅ 能	✅ 属于
内存（用户进程）	✅ OS内核	✅ 知道	❌ 不能	❌ 不属于
数据库连接池	✅ 连接池管理器	✅ 知道	✅ 能	✅ 属于


溢出的池概念 回归肢端耗尽

解析带来的新价值

它定义了资源管理的边界

能管的：池耗尽（主动管理）

管不了的：肢端耗尽（被动容错）

这本身就是一种极简的“管理哲学”。

它揭示了“管理者”的核心地位

没有管理者，就没有池。

池的范围，由管理者的认知边界决定。

这让你在设计系统时，能清晰地知道“我要管什么，我不用管什么”。

它提供了“溢出”的自然衔接

溢出不是失败，是模式的切换。

从“精细控制”优雅降级到“启发式容错”。

这让系统在面对未知和变化时，依然稳健。

它把“CPU是否属于池耗尽”这类问题，变成了一个可判断的标准问题

有调度者吗？能识别吗？能控制吗？

三个问题，答案自现。

✅ 最终价值

“如何思考资源管理”的元认知工具。

它帮助你（和未来的使用者）在面对任何资源管理问题时，都能：

识别：谁是管理者？资源池的边界在哪？

分类：这个资源是高可复用还是低可复用？核心还是边缘？

决策：该用池耗尽精细管理，还是用肢端耗尽启发式容错？

衔接：当管理失效时，如何优雅降级？